import * as React from "react";
import { AppLayout } from "../layouts";

export const ListScene: React.FC = () => {
  return (
    <AppLayout>
      <h1>I´ll be a list in future.</h1>
    </AppLayout>
  );
};
